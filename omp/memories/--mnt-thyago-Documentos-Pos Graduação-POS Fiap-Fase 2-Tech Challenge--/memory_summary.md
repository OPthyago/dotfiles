User's main project: TECH-CHALLENGE-FASE-2 (FIAP Fase 2 pos-graduação), a Python data engineering pipeline for INEP alfabetização data using AI-DLC methodology (Units of Work U1, U2...) and learning-oriented (user implements, not full automation). Key rules: never commit/gitignore aidlc-docs/; Makefile-based workflow (no Airflow); Pydantic/PyArrow/Pytest/Hypothesis stack. Known blocker: `make install` fails because pip isn't on PATH — needs python3 -m pip or venv fix. Data schema notes: BigQuery basedosdados.br_inep_avaliacao_alfabetizacao — 'municipio' table is aggregated eval data, NOT a name/UF lookup; need external basedosdados.br_bd_diretorios_brasil.municipio for Silver layer. U1 (bronze models) in progress, U2 (Terraform infra) planned next.

Also has OMP (oh-my-pi) tooling knowledge: compaction strategies (shake vs context-full vs handoff), when to use manual /shake (only near 70-80% context usage), extension auto-discovery requires package.json 'omp.extensions' manifest, and preference for built-in defaultThinkingLevel:auto over custom model-router extensions.
