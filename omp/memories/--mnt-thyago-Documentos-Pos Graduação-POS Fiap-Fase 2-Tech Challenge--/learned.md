- Installing graphify (Graphify-Labs/graphify) as an OMP-wide managed skill: pip package graphifyy installed via uv tool install graphifyy (CLI command stays graphify), skill body adapted from the vendor's skill-pi.md bundle with Claude-Code-specific "Agent tool / subagent_type=general-purpose" language rewritten to OMP's task tool (default general-purpose worker, tasks[] batch, avoid scout for write-needing chunk subagents). Reference sidecar files (references/*.md) copied verbatim from graphify's skills/pi/references bundle since they are tool-agnostic bash/python.
