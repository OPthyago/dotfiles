User (thyagoop) runs Linux+Sway, uses OMP agent CLI (~/.omp/agent) and LM Studio (local + LAN qwen models) with configurable model roles (default/smol/slow) and a reasoning/thinking toggle that requires both model-config AND harness config to be set. Has a NAS (192.168.0.11, SSH via ~/.ssh/nas_claude key, port 44, user bolseiro, sudo needs password, docker not in PATH) running Paperless-ngx (MCP token currently expired/401) and evaluating Plane (primary) or Kan (lightweight fallback) as self-hosted MCP-controllable kanban/task tools, with a known Google Calendar sync gap requiring a parallel GCal MCP or n8n/Make bridge. Also has reference fixes: Cider music player freezing due to swaync DBus hangs (fix: restart swaync / disable notifications), and Royal Enfield Meteor 350 OBD2 starting-issue diagnostics (relay grease, battery terminals, side-stand sensor). Always verify model-specific/generation-specific info via web search rather than generic tables; verify server reachability with curl before assuming client config issues.
